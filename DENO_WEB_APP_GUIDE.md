# Deno Webアプリケーション 構成ガイド

Denoを使用したWebアプリケーションの標準的な構成と、「Deno + SQLite + vanilla
js」というスタックの評価についてまとめます。

---

## 1. Deno Webアプリケーションの標準的な構成

Denoでは、特定のフレームワーク（Hono,
Freshなど）を使用する場合でも、以下の構成が「平均的かつモダン」とされています。

### ディレクトリ構成例

```text
.
├── src/                # ソースコード
│   ├── main.ts         # エントリーポイント（サーバー起動）
│   ├── routes/         # ルーティング定義
│   ├── controllers/    # ビジネスロジック
│   ├── models/         # データモデル（SQLiteの定義など）
│   ├── services/       # 外部APIやデータベースへのアクセス
│   └── utils/          # 共通ユーティリティ
├── static/             # 静的ファイル（HTML, CSS, クライアント用JS, 画像）
│   ├── index.html
│   ├── css/
│   └── js/
├── tests/              # テストコード（*.test.ts）
├── deno.json           # プロジェクト設定、タスク、依存関係の管理
├── .env                # 環境変数
└── .env.example        # 環境変数のテンプレート
```

### deno.json の設定例

Deno 2.0以降、`deno.json`
で依存関係（imports）やタスク（tasks）を管理するのが標準です。

```json
{
  "tasks": {
    "dev": "deno run --watch -A src/main.ts",
    "test": "deno test -A",
    "lint": "deno lint",
    "fmt": "deno fmt"
  },
  "imports": {
    "std/": "https://deno.land/std/",
    "hono": "jsr:@hono/hono",
    "sqlite": "https://deno.land/x/sqlite/mod.ts"
  },
  "fmt": {
    "indentWidth": 2,
    "useTabs": false
  }
}
```

---

## 2. 「Deno + SQLite + vanilla js」スタックの評価

この構成は、軽量さとポータビリティを重視した「質実剛健」な選択です。

### 評価まとめ

| 項目                   | 評価      | 理由                                                           |
| :--------------------- | :-------- | :------------------------------------------------------------- |
| **開発速度**           | 高 (初期) | セットアップがほぼ不要で、すぐに書き始められる。               |
| **実行パフォーマンス** | 極めて高  | オーバーヘッドが最小限。SQLiteのローカルアクセスは非常に高速。 |
| **保守性**             | 中        | vanilla jsの規模が大きくなると管理が難しくなる可能性がある。   |
| **ポータビリティ**     | 最高      | シングルバイナリ（Deno）と単一ファイル（SQLite）で完結する。   |

### メリット

1. **ゼロ・コンフィギュレーション**: ビルドツール（Webpack,
   Viteなど）が不要で、ブラウザが直接JSを解釈するため、デバッグが容易です。
2. **単一ファイルのDB**:
   SQLiteはファイル1つで管理できるため、バックアップや移動が非常に簡単です。Denoとの相性も良く、`x/sqlite`
   などのドライバで手軽に扱えます。
3. **将来性**: Web標準（vanilla
   js）に準拠しているため、フレームワークの流行り廃りに影響されにくいです。

### デメリット・注意点

1. **状態管理**: vanilla
   jsではDOM操作が煩雑になりがちです。複雑なUIを作る場合は、Web
   Componentsを活用するか、軽量なライブラリ（Preactなど）の検討が必要になるかもしれません。
2. **SQLiteの制限**:
   書き込みが集中する大規模な多人数同時接続アプリには向きません（ただし、読み取りは非常に高速です）。

---

## 3. ベストプラクティス

### テスト

Deno組み込みのテストランナー（`deno test`）を活用します。

- `Deno.test()` を使用してバックエンドのロジックをテスト。
- SQLiteのテストでは、インメモリDB（`:memory:`）を使用することで、ファイル汚染を防ぎつつ高速にテストを実行できます。

### デプロイ

- **Deno Deploy**:
  最も親和性が高い。ただし、SQLite（ローカルファイル）を使用する場合は、Deno
  KVへの置き換え、あるいはPersistentなディスクを持つVPS/Docker環境が必要になります。
- **Docker**: `denosland/deno`
  公式イメージを使用し、SQLiteファイルをボリュームマウントする構成が一般的です。

### セキュリティ

- Denoの権限フラグ（`--allow-read`, `--allow-net`
  等）を最小限に絞って実行します。
- SQLiteへのアクセス権限を適切に管理します。
